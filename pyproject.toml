[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "etl_library"
version = "0.1.0"
description = "A simple ETL library for Tomba, Hunter, and Leadfeeder"
dependencies = [
    "requests>=2.31.0",
]

[tool.setuptools.packages.find]
where = ["."]